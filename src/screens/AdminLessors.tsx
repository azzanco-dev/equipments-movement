// The lessors list now lives in `src/screens/lessors/`. This module keeps the
// original export name so routing in `src/App.tsx` is unchanged.
export { LessorsListScreen as AdminLessors } from './lessors/LessorsListScreen'
