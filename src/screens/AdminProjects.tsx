// The projects list now lives in `src/screens/projects/`. This module keeps
// the original export name so routing in `src/App.tsx` is unchanged.
export { ProjectsListScreen as AdminProjects } from './projects/ProjectsListScreen'
