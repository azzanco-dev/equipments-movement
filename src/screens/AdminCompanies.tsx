// The companies list now lives in `src/screens/companies/`. This module keeps
// the original export name so routing in `src/App.tsx` is unchanged.
export { CompaniesListScreen as AdminCompanies } from './companies/CompaniesListScreen'
